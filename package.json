{
  "name": "root",
  "private": true,
  "engines": {
    "node": "^14.15.1"
  },
  "scripts": {
    "lint": "yarn eslint . --ext ts,js",
    "lint:fix": "yarn lint --fix"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
