{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "yarn eslint . --ext ts,js",
    "lint:fix": "yarn lint --fix"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
